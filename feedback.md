
# AI Feedback: Leap Year Checker (Python)

**Overall Score:** 65/100 (Good)

---

## Summary

The submission demonstrates a correct leap year algorithm with good code structure, but it fails to meet key assignment requirements for input/output handling and exact output format, resulting in significant gaps.

---

## Strengths

- Correct implementation of leap year algorithm with proper conditional nesting
- Good code organization with a dedicated function and docstring
- Includes manual testing to verify logic with multiple test cases
- Incorrect output format; prints test results instead of required strings

---

## Improvement Areas

- Missing user input handling as per assignment requirements — Add code to read an integer from input using input() and int() conversion, then call is_leap_year() with that value, as specified in README.md.
- Incorrect output format; prints test results instead of required strings — Modify the output to print 'Leap year' if is_leap_year returns True, and 'Not a leap year' if it returns False, matching the exact format in assignment examples.
- Lack of input validation and error handling — Add try-except blocks to handle invalid inputs (e.g., non-integer strings) and provide clear error messages to the user.
- Missing user input handling as per assignment requirements
- Incorrect output format; prints test results instead of required strings
- Lack of input validation and error handling

---

## Immediate Next Steps

- Missing user input handling as per assignment requirements: Add code to read an integer from input using input() and int() conversion, then call is_leap_year() with that value, as specified in README.md.
- Incorrect output format; prints test results instead of required strings: Modify the output to print 'Leap year' if is_leap_year returns True, and 'Not a leap year' if it returns False, matching the exact format in assignment examples.
- Lack of input validation and error handling: Add try-except blocks to handle invalid inputs (e.g., non-integer strings) and provide clear error messages to the user.

---

## Longer-Term Focus

- Add code to read an integer from input using input() and int() conversion, then call is_leap_year() with that value, as specified in README.md.
- Modify the output to print 'Leap year' if is_leap_year returns True, and 'Not a leap year' if it returns False, matching the exact format in assignment examples.
- Add try-except blocks to handle invalid inputs (e.g., non-integer strings) and provide clear error messages to the user.

---

*Generated by RepodIn AI*
**Student:** Arto Matilainen · **Repository:** `leap-year-checker-python-artomatilainen`
**Course:** Python · **Assignment:** Leap Year Checker (Python)
**Model:** RepodIn AI · **Generated:** Mar 26, 2026, 10:28 AM
